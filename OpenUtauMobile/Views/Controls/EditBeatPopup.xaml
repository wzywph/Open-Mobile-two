<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
               xmlns:vm="clr-namespace:OpenUtauMobile.ViewModels.Controls"
               xmlns:io="clr-namespace:System.IO;assembly=System.Runtime"
               xmlns:res="clr-namespace:OpenUtauMobile.Resources.Strings"
                xmlns:converters="clr-namespace:OpenUtauMobile.ViewModels.Converters"
               Color="Transparent"
               x:Class="OpenUtauMobile.Views.Controls.EditBeatPopup">

    <!-- 主体 -->
    <Border StrokeThickness="0"
            Background="{DynamicResource PopupBackground}"
            Padding="0"
            WidthRequest="400"
            StrokeShape="RoundRectangle 10">
        <Grid RowDefinitions="40, Auto, 60">
            <!-- 标题栏 -->
            <Border BackgroundColor="#66CCFF" Padding="10,2,10,2">
                <Label x:Name="LabelTitle" Text="{Static res:AppResources.SetBeat}" FontSize="20" HorizontalTextAlignment="Start" VerticalTextAlignment="Center"/>
            </Border>
            <!-- 输入框 -->
            <HorizontalStackLayout Grid.Row="1">
                <Entry x:Name="EntryBeatPerBar" Margin="10,20,10,20" Keyboard="Numeric"/>
                <Label Text=" / " VerticalTextAlignment="Center" FontSize="20"/>
                <Entry x:Name="EntryBeatUnit" Margin="10,20,10,20" Keyboard="Numeric"/>
            </HorizontalStackLayout>
            <!-- 按钮栏 -->
            <HorizontalStackLayout Grid.Row="2" Margin="10" HorizontalOptions="End" Spacing="10">
                <Button Text="{Static res:AppResources.CancelText}" Clicked="OnCancelClicked" CornerRadius="25" WidthRequest="80" BackgroundColor="Transparent" TextColor="{DynamicResource Primary}"/>
                <Button Text="{Static res:AppResources.Confirm}" Clicked="OnConfirmClicked" CornerRadius="25" WidthRequest="80"/>
            </HorizontalStackLayout>
        </Grid>
    </Border>
</toolkit:Popup>
